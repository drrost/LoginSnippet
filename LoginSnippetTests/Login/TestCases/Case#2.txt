
Status: DONE

Case #2: Enter wrong email and wrong password
STR:
- Type "aaa"
- Tap `Next`, EX1
- Type "bbb"
- Tap `Done` EX2

EX1:
- `email` field should have red border and red text color
- `password` field should have gray border and "password" as placeholder text
- `password` field should be active, keyboard should be shown
- `login` button shouldn't be inactive

EX2:
- `email` field should have red border and red text color
- `password` field should have red border and red text color
-  keyboard should be hidden
- `login` button shouldn't be inactive
